server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: campaign-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hireblind_campaign}
    username: ${SPRING_DATASOURCE_USERNAME:hireblind}
    password: ${SPRING_DATASOURCE_PASSWORD:hireblind_dev}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET:hireblind-phase1-shared-hmac-secret-key-that-is-at-least-256-bits-long}

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.hireblind.campaign: DEBUG
